name: Automatic Workflow

on: 
  push:  # can trigger when push
    branches: [main]
  workflow_dispatch:   # can be triggered manually from the Actions tab in GitHub
    

jobs: 
  automatic-workflow-job:
    runs-on: ubuntu-24.04
    steps: 
      - run: echo "This workflow runs automatically when you push to the main branch."